type Mutation {
  signUp(
    firstname: String!
    lastname: String!
    username: String!
    password: String!
  ): StatusPayload

  login(username: String, password: String): TokenPayload
}

type StatusPayload {
  success: Boolean!
}

type TokenPayload {
  token: String!
}